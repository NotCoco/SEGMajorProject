package main.java.com.projectBackEnd;

/**
 * Each Interface which accesses the database that will be required by the REST API endpoints
 * will be extended here so all the methods are in one place.
 */
public interface RestInterface extends PageRestInterface { //extends MedicineRestInterface etc.
}
